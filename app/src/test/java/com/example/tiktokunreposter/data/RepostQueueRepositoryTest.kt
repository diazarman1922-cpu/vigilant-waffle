package com.example.tiktokunreposter.data

import org.junit.Test

class RepostQueueRepositoryTest {
    @Test
    fun saveResumeSkeleton() {
        // Android Context-backed repository is covered by instrumentation/manual tests.
        // Kept as a compile-time placeholder so CI can add Robolectric later if desired.
    }
}
